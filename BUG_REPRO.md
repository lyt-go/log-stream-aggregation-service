# 采集汇总超时

## Bug 是什么

一批采集中包含被拒绝的记录时，汇总接口不会返回采集器拒绝信息，而是一直等待到请求超时。随后提交的正常批次也可能无法返回完整结果。

## 如何触发

先提交一条正常日志，再提交一条被采集器拒绝的记录；等待请求 deadline。随后在新的批次中只提交正常日志。

## 错误信息

`pipeline stopped: context deadline exceeded`
